const mainBackgroundColor = '#fff'

export default {
   common: {
      backgrounds: {
         mainBackgroundColor,
      },
      spinnerColor: 'blue',

      elevatedView: {
         shadowColorIOS: '#000',
         defaultBackgroundColor: mainBackgroundColor,
      },
   },

   asserts: {
      selectedAssetBorderColor: '#00f',
   },
}
